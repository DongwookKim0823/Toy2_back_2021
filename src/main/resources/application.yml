spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://study.cx0gnyh6h4ss.ap-northeast-2.rds.amazonaws.com:3306/study?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: study
    password: 12345678

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true

logging:
  level:
    org.hibernate.SQL: debug
